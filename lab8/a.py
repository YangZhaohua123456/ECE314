
import matplotlib.pyplot as plt
import numpy as np
import scipy as sp
import scipy.stats as st
import csv as csv
import networkx as nx
#a=np.random.choice(3, p=[0.1,0.2,0.7])
#print(a)
#print(np.identity(4))
#G = nx.scale_free_graph(10, alpha=0.2, beta=0.4, gamma=0.4)   
#nx.draw(G,with_labels=1)
#plt.show()
#n = G.number_of_nodes()
#A = np.array(nx.adjacency_matrix(G).toarray()) + np.identity(n)
#d=0.85
#print(np.new)
#P=d*A/A.sum(axis=1)[:,np.newaxis]  + ((1-d)/n)*np.ones((n,n))
#P=d*A/A.sum(axis=1)  + ((1-d)/n)*np.ones((n,n))
#print(P)
#p=np.array([0.2,0.2,0.4,0.2])
#c=np.arange(np.size(p))
#print(c)
print(np.random.randint(0,3))